# 更新日志（CHANGELOG）

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)，所有显著变更记录于此。

## v1.3.1（2026-09-19）

> 主题：**客户端编辑锁修复 + 客户端回收站/历史版本 + 打包依赖修复**。

### 修复

- **客户端部门文件编辑锁残留**：打开文件 → 保存并关闭编辑器后，服务端编辑锁依然存在（客户端不感知外部程序关闭，「空保存」内容无变化也不会触发释放）。现在心跳周期内探测缓存文件是否仍被编辑器独占：连续 2 轮（约 60~90s）未被占用即自动释放锁并停止续约；轻量编辑器（不持续持有句柄）误判提前释放由「保存回传前重新抢锁」兜底，数据安全不受影响。
- **requirements.txt 缺少 `cryptography` 显式声明**：备份密码 Fernet 加密依赖此前被 `paramiko` 的传递依赖掩盖，纯净环境 `pip install -r requirements.txt` 后后端启动报 `ModuleNotFoundError: No module named 'cryptography'`。已显式声明 `cryptography==50.0.1`。

### 客户端

- 新增**回收站**：桌面客户端可查看/还原/彻底删除/清空回收站（首页操作个人盘、部门网盘操作所选部门），显示删除人与剩余保留天数；个人盘变更后自动触发一次同步对齐本地文件夹。
- 新增**文件历史版本**：部门网盘文件行新增「版本」入口，可查看版本列表（版本号/大小/修改人/保存时间）、下载任意历史版本到本地、一键恢复（当前内容自动另存为新版本）。

## v1.3.0（2026-09-19）

> 主题：**回收站 + 文件历史版本 + 监控仪表盘 + 备份恢复**。数据安全与运维可观测性增强；备份目标支持本地服务器与远端服务器（SFTP / FTP）。

### 新功能

#### 回收站（个人盘 + 部门盘）

- 删除文件 / 文件夹改为**软删除**：进入回收站并记录删除人、原位置与到期时间，默认保留 **30 天**，到期由定时任务自动彻底清除
- 回收站支持**按原位置还原**、单条**彻底删除**与**清空回收站**；个人盘与部门盘回收站相互独立，部门回收站沿用部门读写权限
- 后台独立开关（默认开启）；**关闭后新删除走硬删除，但不清毁回收站存量**，重新开启即可看到历史数据
- 新增数据表 `trash_item`；`file_node` 新增 `deleted_time` / `delete_operator_id`，启动时幂等自动迁移

#### 文件历史版本

- 文件被**覆盖上传**时自动保留旧版本（含修改人、大小、哈希、时间），默认每文件保留 **10 个版本 / 30 天**，超量与到期自动清理
- 版本列表支持**旧版本下载**与**一键恢复**（恢复动作本身再生成一个版本，可来回切换）
- 物理文件沿用 v1.2.0 内容寻址 blob：每多一个版本多一份引用计数，版本清理 / 彻底删除后引用归零才回收磁盘
- 后台独立开关（默认开启），关闭后不产生新版本，存量版本仍可查看 / 下载 / 恢复

#### 管理后台监控仪表盘

- **CPU / 内存 / 磁盘**实时指标：容器内自动读取 **cgroup v1/v2 限额**，正确显示配额核数与容器内存占用（基于 psutil）
- **5 分钟在线人数**（15 秒采样心跳去重）、**今日上 / 下行流量**、**近 7 天流量**双柱图（纯 SVG，无新增前端依赖）
- **存储统计**：已用空间 / 总配额进度条、个人盘 / 部门盘占用、文件数、文件夹数、用户数、部门数
- 页面支持 30 秒自动刷新；磁盘占用 ≥90% 红色、≥70% 黄色预警

#### 备份与恢复

- **一键手动备份**与**每日定时备份**：调度器每小时 :17 唤醒，任务内自查当天执行小时与开关，改时间无需重启服务；支持保留份数（自动淘汰最旧备份）
- 备份内容为**数据库（元数据）+ 安装配置**，**不含文件实体**（文件实体在存储卷 / 对象存储中，请另行做磁盘级备份）；包内附 `manifest.json` 与逐文件 SHA-256，坏包 / 被篡改包拒绝恢复
- 备份目标可选：
  - **本地服务器**：存入 `storage/backups/`
  - **远端服务器**：协议 **SFTP（推荐，基于 paramiko）/ FTP**，可配置 IP / 主机、端口、用户名、密码、远端目录；密码以 **Fernet 对称加密**存储（密钥由 SECRET_KEY 派生），接口永不回传明文
  - 支持**连接测试**（仅测试不保存）；远端目录不存在时自动逐级创建
  - 远端备份采用「本地先打包 → 上传」：上传失败保留本地包并在备份列表中标注失败原因
- **恢复**：支持从服务器备份记录、游离本地备份文件、远端文件名、**上传 zip 包**四种入口；恢复前自动生成 pre-restore 回滚快照；恢复成功 2 秒后进程自动退出，由 supervisor / systemd / Docker 守护拉起
- 数据库支持 **SQLite 与 MySQL（含 Docker 内置 MariaDB，mariadb-dump / mysqldump 双兼容）**；PostgreSQL 明确返回「暂不支持」，不会误操作
- 新增数据表 `backup_record`

#### Web 管理端

- 「系统管理」菜单组：新增**监控仪表盘**、**备份恢复**、**系统设置**三个页面
- 系统设置：注册开关、回收站 / 版本策略（开关与保留参数）、备份策略（开关、每日小时、保留份数、本地 / 远端、远端连接表单与测试连接）
- 备份恢复页：立即备份（进度轮询）、备份列表（类型 / 本地或远端位置 / 大小 / 状态）、下载、恢复（强制风险确认勾选）、删除、上传备份包恢复
- 个人盘与部门盘工具栏新增**回收站**入口，文件行新增**历史版本**入口；删除确认文案随回收站开关动态变化
- 桌面客户端本轮无 UI 变更

#### 接口与定时任务

- 新增接口：`GET /api/admin/metrics`、`POST /api/admin/backup`、`POST /api/admin/backup/test-connection`、`GET /api/admin/backups`、`GET /api/admin/backup/download/<id>`、`DELETE /api/admin/backup/<id>`、`POST /api/admin/backup/restore`
- 新增接口：`GET /api/file/trash`、`POST /api/file/trash/restore`、`DELETE /api/file/trash/item`、`DELETE /api/file/trash`、`GET /api/file/versions/<node_id>`、`GET /api/file/version/download`、`POST /api/file/version/restore`
- `GET /api/system/feature-flags` 增加 `trash_enabled` / `version_enabled`
- 定时任务新增：回收站清理、版本清理、指标采样（15 秒）、备份调度（每小时 :17）
- 错误码：设置校验 1221 / 1222 / 1223；备份恢复 3601 忙碌 / 3602 失败 / 3603 连接失败 / 3604 不存在 / 3605 坏包 / 3606 数据库不支持 / 3607 未确认风险

### 修复与加固

- 修复 Docker（MySQL）恢复时导入客户端等待本进程连接池**表元数据锁（MDL）**导致 worker 超时的问题：导入前统一释放 SQLAlchemy 连接池
- 修复恢复数据库后 Redis 中旧的设置 / 插件 / JWT 吊销缓存覆盖回滚数据的问题：恢复成功后自动清理相关缓存
- 补齐本地游离备份文件（恢复后旧备份记录随旧库消失）的恢复入口，前端无需区分本地 / 远端
- 设置保存改为**两阶段校验**：全部字段校验通过后才写入，避免远端配置半写入；远端目标即使本次未提交连接字段也校验完整性
- 流量统计日 key 清理修正 LIKE 转义；同秒多次备份文件名自动追加序号避免唯一约束冲突

### 升级说明（1.2.0 → 1.3.0）

1. 拉取新代码后按原有方式重启即可，数据库**首次启动自动幂等迁移**（加列 + 新建 3 张表），旧数据不受影响
2. 新增 Python 依赖 `psutil==6.1.0`、`paramiko==3.5.0`（均为纯 Python / 通用 wheel，Docker 镜像已内置，无需额外 apt 安装）
3. 回收站与历史版本**默认开启**，关闭开关不会删除存量数据；保留天数 / 版本数可在「系统设置」调整
4. 备份只含数据库与配置，**文件实体请通过存储卷快照 / 磁盘备份另行保障**；Docker 部署建议同时备份 `app-storage` 与 `app-mysql` 两个数据卷
5. 恢复机制依赖进程守护自动拉起：Docker（supervisor）与 systemd / supervisor 部署开箱即用；裸机 `python run.py` 前台运行需手动重启
6. 远端备份推荐使用 SFTP（密码认证）；FTP 为兼容选项，传输不加密，仅限可信内网

### 质量保障

- 后端 pytest **206 项**（新增指标 / 设置校验 / SQLite 备份往返 / 坏包 / 淘汰 / PG 拒绝 / 内嵌 SFTP / FTP 服务器 / 远端上传 / 恢复全流程等 **18 项**）
- Web 端 vitest **25 项**通过，生产构建通过；浏览器冒烟覆盖监控仪表盘、备份全流程、系统设置本地 / 远端切换、回收站、版本对话框
- 桌面客户端 vitest **127 项**通过（与 1.2.0 基线一致，客户端无功能变更）
- 真实 Docker 镜像（内置 MariaDB 11）E2E：mariadb-dump 备份成功、按记录恢复 2 秒内 supervisor 拉起、设置值正确回滚、本地游离文件恢复成功、零 worker 超时

## v1.2.0（2026-09-18）

> 主题：**跨用户秒传 + 大文件断点续传**。同一份文件在全服务器只存一份物理副本；大文件上传中断可续传，下载支持 HTTP Range 断点恢复。

### 新功能

#### 跨用户文件秒传（物理去重）

- **MD5+大小双因子指纹**：任意用户上传前先查全局 `file_blob` 指纹库，命中则零带宽直接建立文件引用，任务状态显示「秒传」
- **引用计数物理去重**：同一物理文件全服务器仅落盘一份（`storage/blobs/<hash前2位>/<hash>`），各用户/部门仍按逻辑占用各自配额；最后一个引用删除后物理文件才回收
- **并发安全**：两人同时上传同一文件时数据库唯一约束 + 原子引用计数保证只落一份；引用归零删除带 `ref_count > 0` 守卫
- 覆盖、删除、共享副本场景均保持配额与物理文件正确（共享文件任一方删除不影响另一方）

#### 大文件分片上传 / 断点续传

- 文件 **大于 20MB** 自动启用分片上传：固定 **5MB/片**，并发 3、单片失败自动重试 3 次，实时进度
- **断点续传**：上传中断（网络抖动、客户端退出）后重新上传同一文件，服务端按「同用户+同内容+同目标」恢复会话，仅补传缺失分片；会话 24h 有效
- **完整性校验**：单片 MD5 校验 + 末片余数大小校验 + 合并后整文件 MD5 校验，任何一环不符即拒绝并标记会话失效
- 新增接口：`POST /api/file/instant`、`POST /api/file/chunk/init|upload|complete|abort`、`GET /api/file/chunk/status`；`/api/file/upload`、`/api/file/check-duplicate` 行为完全向后兼容

#### 下载断点续传

- 下载接口支持标准 HTTP `Range` 请求（返回 `206 Partial Content`）
- 桌面客户端下载中断后从 `.zhy.part` 断点追加，并以 `.zhy.meta` 防止不同文件的临时文件串用；完成后校验大小与 MD5

#### 存量收敛与自动清理

- **blob 收敛任务**（每日）：自动为 1.0/1.1 旧文件补算 MD5，将同内容的多个物理副本收敛为 blob 单份，并对账修正引用计数、清理无主 blob
- **暂存区 GC**（每 10 分钟）：清理过期分片会话、残留临时分片与孤儿暂存目录

#### 部门文件排他编辑锁

- 有读写权限的用户在桌面客户端双击打开部门文件时自动获取**排他编辑锁**：打开期间其他读写用户对该文件降级为**只读**（文件右下角显示红色锁角标，悬停可见持有者），防止多人同时编辑覆盖冲突
- 客户端抢锁失败会以只读方式打开并提示「文件正被 XX 编辑」，只读文件本地置只读位且绝不回传；**保存回传成功后立即释放锁**，其他用户随即恢复可编辑；继续再编辑保存时自动重新抢锁
- 编辑期间客户端每 **30 秒心跳续约**（锁 TTL 2 分钟），退出/关闭客户端兜底释放；客户端崩溃等异常由 TTL 自动过期 + 每 5 分钟清理任务回收；超管/部门管理员可强制释放他人锁
- 服务端硬拦截：锁被他人持有时，覆盖保存 / 重命名 / 移动 / 删除（含包含被锁文件的文件夹）一律返回 `3501` 并提示持有者；下载/在线预览等读操作不受影响
- 新增接口：`POST /api/file/lock/acquire|heartbeat|release`、`GET /api/file/lock/status`；`/api/file/list` 部门列表每项新增 `locked`、`locked_by_me`、`lock_user_name` 字段

#### Web 管理端

- 上传任务新增「秒传」状态（秒传成功 / 内容一致跳过）
- 大于 20MB 文件自动分片上传，进度条正常显示，取消任务可中断当前分片

#### 桌面同步客户端

- 个人同步与部门网盘上传统一走「秒传 → 分片续传 → 整文件」三级通道（覆盖上传保持原整文件通道）
- 个人同步、部门下载、双击打开缓存全部支持下载断点续传与完成校验
- 部门上传进度弹窗新增「秒传完成，零上传流量」/ 分片续传提示

#### Docker 单容器部署

- 官方镜像改为 **all-in-one 单容器**：内置 MySQL + Redis + nginx + gunicorn（supervisor 统一托管），无需再单独部署 / 连接数据库
- 首次启动自动完成 MySQL root 授权、建库与静默安装；安装向导在 Docker 模式下跳过数据库 / Redis 配置步骤，直接进入管理员配置
- 镜像已发布至 Docker Hub `pollybird/zhy-cloud-disk`（`latest` / `1.2.0` 标签），可直接 `docker run` 部署
- `docker-compose.yml` 简化为单服务（数据卷 `app-instance` / `app-storage` / `app-mysql`），`.env.example` 同步精简，`deploy.sh` 适配单容器
- 修复同步盘「pipeline is not defined」与变量遮蔽（TDZ）两个导致同步失败的缺陷

### 升级说明（1.1.0 → 1.2.0）

1. 拉取新代码后按原有方式重启即可，**数据库首次启动自动幂等建表**（`file_blob`、`upload_session`、`upload_chunk`、`file_lock`），旧数据不受影响
2. 历史物理文件无需手动处理：服务运行后由每日 blob 收敛任务逐步去重，期间功能正常
3. nginx 已配置 `client_max_body_size 2g`，分片为普通 5MB multipart 请求，无需调整反代
4. 建议升级前备份数据：手动 / SQLite 部署备份 `backend/instance` 目录与存储目录；Docker 单容器部署备份 `app-mysql`（MySQL 数据）与 `app-storage`（用户文件）两个数据卷
5. Docker v1.2.0 为全新单容器镜像（内置 MySQL），数据卷结构与 v1.1.0 多容器（SQLite + 独立 redis）不同，不自动迁移旧容器数据，建议作为全新部署使用

### 质量保障

- 后端 pytest **176 项**（新增跨用户秒传 / 引用计数 / 分片续传 / Range 下载等 **15 项**，排他编辑锁 **13 项**）
- Web 端 vitest **25 项**（新增分片编排器 **8 项**）
- 桌面客户端 vitest **127 项**（新增分片上传 / Range 下载 **17 项**、编辑锁生命周期 **9 项**）
- 真实服务 E2E：跨用户秒传 0 字节、200MB 分片中断后续传 MD5 一致、Range 206 下载中断恢复、配额与引用计数核对；排他锁双用户互斥 / 写拦截 / 保存释放 / TTL 接管共 **18 项**

## v1.1.0（2026-09-17）

> 主题：**多级部门共享网盘**。核心原则：安装时未开启部门共享 = 1.0.0 功能完全不变；开启后新增部门维度，个人网盘完全兼容。

### 新功能

#### 多级部门共享网盘（可选功能）

- **功能开关**：安装向导勾选开启，或安装后在系统设置中切换；Docker 静默安装支持环境变量 `ZHY_DEPARTMENT_DRIVE=true`，默认关闭
- **无限级部门树**：新增 / 重命名 / 迁移 / 删除部门，同级不重名，支持同级排序与部门独立存储配额（`0` 表示不限额）
- **成员与管理员**：部门成员可设职位；部门管理员分级委派——仅本部门 / 本部门及子部门
- **细粒度权限**：只读 / 读写 / 拒绝三级权限，支持文件夹级授权与到期时间；优先级：拒绝 > 显式授权 > 成员默认只读
- **部门文件管理**：上传（MD5 秒传去重、配额原子扣减）、文件夹递归创建、改名、移动、下载、删除；部门文件与个人文件物理目录、配额、权限完全隔离
- **操作审计日志**：记录部门内上传、删除、移动、授权等关键操作
- **关闭时的行为**：部门相关接口统一返回 HTTP 403（业务码 4030），个人网盘与 1.0.0 行为完全一致

#### Web 管理端

- 新增「部门网盘」页面：部门树 + 权限标签 + 配额管理，资源管理器式文件列表
- 文件分类彩色图标（办公文档 / 图片 / 视频 / 压缩包等 8 类）
- 上传支持按钮选择与拖拽；删除非空部门有明确拦截提示（业务码 3310）

#### 桌面同步客户端

- **部门网盘浏览**：按权限显示可见部门树，资源管理器式操作，不占用本地同步目录
- **双击编辑回传**：双击文件下载到临时目录并用本机程序打开，保存后自动回传（需读写权限）；只读用户可另存到本地
- **上传下载**：多选文件/文件夹下载到本地目录；按钮与拖拽上传（含文件夹）到部门目录
- **临时缓存管理**：退出客户端自动清理，超期缓存自动删除
- **修复**：配置残缺（已配置但缺少同步路径）时进入主界面后所有请求报 "Invalid URL" 的问题，现在会强制回到首次配置向导

### 升级说明（1.0.0 → 1.1.0）

1. 拉取新代码后按原有方式重启即可，**1.0.0 数据库首次启动自动完成幂等迁移**（补建部门相关表、补齐 `file_node.file_hash` / `file_node.department_id` / `user.primary_department_id` 列），旧数据与个人功能不受影响
2. 部门功能默认关闭，需在安装配置或系统设置中开启
3. 建议升级前备份 `backend/instance` 目录

### 部署

- 新增一键部署脚本 `deploy.sh`（`init` / `up` / `down` / `restart` / `status` / `logs` / `update` / `shell`，含依赖检查、健康检查与安全告警）与 `.env.example` 配置模板
- `docker-compose.yml` 新增 `ZHY_DEPARTMENT_DRIVE` 变量透传（默认 `false`），Docker 静默安装时可开启部门网盘
- 桌面客户端服务器地址填 Web 访问地址即可（如 `http://<服务器IP>:8080`），经 nginx `/api/` 反代访问后端，无需单独开放后端端口
- 新增可选后端端口映射 `ZHY_BACKEND_PORT`（默认仅绑定宿主机回环口 `127.0.0.1:5000`，排障直连时可设 `0.0.0.0:5000`）
- 公网部署建议前置 HTTPS 反向代理

### 性能

- 部门树查询深度优化：超管场景无 N+1，实测 7 层 127 部门树查询约 **4ms**
- 权限缓存：部门管理员权限 TTL 60s、文件权限 TTL 30s，授权变更主动失效
- 30MB 文件本机上传/下载约 0.1s；秒传判定 3-4ms

### 质量保障

- 后端 pytest **148 项**、桌面客户端 vitest **100 项**、Web 端 **17 项**单元测试全部通过
- **51 项** API 端到端回归（含权限隔离、删除保护、审计日志）
- **25 项** 1.0.0 旧库兼容回归（自动迁移、关闭态行为、旧文件完整性）
- 桌面客户端 GUI 实测：首次向导、部门浏览、双击编辑回传、上传下载、设置页

## v1.0.0

- 首个公开发布版本：个人文件管理、分享、插件扩展、Docker 一键部署、桌面双向同步客户端
